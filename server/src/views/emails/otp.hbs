<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>OTP - {{appName}}</title>
  <style>
    body { font-family: Arial, sans-serif; background:#f9f9f9; margin:0; padding:0; }
    .container { max-width:600px; margin:30px auto; background:#fff; border-radius:8px; overflow:hidden; box-shadow:0 2px 8px rgba(0,0,0,.1); }
    .header { background:#4a90e2; color:#fff; padding:20px; text-align:center; }
    .header h1 { margin:0; font-size:24px; }
    .content { padding:30px; text-align:center; }
    .otp { font-size:32px; font-weight:bold; letter-spacing:4px; color:#4a90e2; margin:20px 0; }
    .footer { background:#f1f1f1; padding:15px; text-align:center; font-size:12px; color:#777; }
    .btn { display:inline-block; background:#4a90e2; color:#fff; padding:12px 24px; text-decoration:none; border-radius:4px; margin-top:20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>{{appName}}</h1>
    </div>
    <div class="content">
      <h2>Verification Code</h2>
      <p>Use the code below to complete your registration. It expires in <strong>5 minutes</strong>.</p>
      <div class="otp">{{otp}}</div>
      <p>If you didn’t request this, you can safely ignore this email.</p>
      <a href="{{appUrl}}" class="btn">Go to {{appName}}</a>
    </div>
    <div class="footer">
      © {{year}} {{appName}}. All rights reserved.
    </div>
  </div>
</body>
</html>